# Cloudflare Pages.
# Astro 정적 다중 페이지로 전환(레슨별 실제 HTML)했으므로 SPA catch-all rewrite 는 제거한다.
# (구 `/*  /index.html  200` 은 /en·/ja·/c/... 실제 라우트를 덮어써 SEO 를 깨므로 삭제.)
# 알 수 없는 경로는 Astro 가 생성한 404 페이지(추후 src/pages/404.astro, 2단계)로 처리.
