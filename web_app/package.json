{
  "name": "riff-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Riff — 무료 기타 레슨 정적 SPA (바닐라 + Vite + TypeScript). Zero-Cost / Static / i18n-first.",
  "scripts": {
    "build:content": "node scripts/build-content.mjs",
    "dev": "npm run build:content && vite",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run build:content",
    "build": "npm run build:content && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
