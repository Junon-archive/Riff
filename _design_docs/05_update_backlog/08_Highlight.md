---
id: 08-highlight
status: IN_PROGRESS (Phase 0·1 완료, Phase 2 파일럿 1개월 1주 완료 — 나머지 배치 대기)
priority: medium
risk: low
depends_on: []
---

# 08 · 산문 하이라이트 + 커리큘럼 볼드 (레시피 동결)

## 목적 (왜)

레슨 산문이 "글의 벽"이라 핵심이 눈에 안 들어온다. **중요한 부분에 연한 하늘색 하이라이트**를 얹어 훑기(scan) 가능하게 한다.

- **각 day 설명 페이지(레슨 산문 ①이론 ②시각 ③연습 ④팁):** 하이라이트만.
- **커리큘럼 설명 페이지(intro 소개 문단):** 하이라이트 **+ 볼드** 둘 다.

**⚠️ 왜 "레시피 동결"인가:** 대상이 **day 116개 × 3언어 = 348파일**(+커리큘럼 intro 3×3언어)로 방대하다. "매 페이지에서 무엇을 강조할지 그때그때 판단"하면 **뒤로 갈수록 기준이 흔들려**(판단 피로) 앞뒤 페이지 강조 밀도·품질이 들쭉날쭉해진다. 그래서 **판단(레시피 수립)과 실행(적용)을 분리**한다 — 작은 파일럿에서 강조 기준을 **명문 규칙으로 동결**한 뒤, 나머지는 그 규칙만 기계적으로 적용한다. 05_update_backlog 공통 정신("나중에 문서만 보고 이어서")과 동일 — 규칙이 문서에 박제돼 있으면 세션이 끊겨도 동일 품질로 재개된다.

## 현재 상태 (검증된 코드 위치)

| 무엇 | 위치 | 현재 동작 | 함의 |
|---|---|---|---|
| **day 산문 `<mark>` 통과** | `web_app/scripts/build-content.mjs:238,240` `inline()` — "인라인 HTML(<b> 등)은 그대로 통과" | 원본 md의 `<mark>…</mark>` 가 이스케이프 없이 통과 | ✅ day 하이라이트는 **콘텐츠만 추가**하면 됨 |
| **day mark 스타일 존재** | `web_app/src/components/LessonView.astro:239-246` `.prose :global(mark){background:var(--highlight)…}` | mark 배경 = `--highlight` | ⚠️ 색이 **초록**(아래) |
| **day 볼드 이미 사용** | `inline()`가 `**…**`→`<b>` (`build-content.mjs:242`), day md에 `**…**` 약 8290회 | 볼드는 이미 저작에 존재 | day는 볼드 추가 대상 아님(하이라이트만) |
| **`--highlight` = 초록** | `web_app/src/styles/tokens.css:58`(라이트) `:94`(다크) `color-mix(green-500…)` | 유일 소비처가 `.prose mark`(LessonView:240) | 산문 하늘색과 색 결정 필요(아래 색 충돌) |
| **색 범례 SSOT 충돌** | `_design_docs/03_data_schema/color_legend.md:10` "target·highlight(일반 강조)=**초록**" | 스코어 role "highlight"=초록의 SSOT | 산문 하이라이트를 하늘색으로 하면 "highlight" 의미가 스코어(초록)≠산문(하늘)로 갈림 → **별도 토큰** 권장 |
| **하늘색 토큰 후보** | `tokens.css` `--blue-50:#E8F3FF` / `--primary-weak:#E8F3FF` | 연한 하늘색 이미 존재 | 하이라이트 배경 후보 |
| **커리큘럼 intro가 이스케이프됨** | `web_app/src/components/CurriculumView.astro:52` `{introParagraphs.map((p) => <p>{p}</p>)}` | `{p}` 는 HTML **이스케이프** | ❌ intro의 `<mark>`/`<b>` 가 **글자로 노출**됨 → 코드 변경 필요 |
| **intro가 raw로 manifest 전달** | `web_app/scripts/build-content.mjs:554` `intro: curMeta?.intro ?? null` | mdToHtml/inline 안 거침 | intro는 마크다운·HTML 변환 경로가 없음 |
| **`.curr-intro` mark/strong 스타일 없음** | `web_app/src/styles/app.css:258-260` (p 스타일만) | mark·b 스타일 미정의 | 커리큘럼 강조 렌더용 스타일 추가 필요 |

**요약 — 두 표면의 준비도가 다르다:**
- **day 페이지:** 렌더 준비 완료. 색만 하늘색으로 바꾸면 **콘텐츠(`<mark>`) 추가만으로** 동작.
- **커리큘럼 페이지:** intro가 이스케이프·미변환·미스타일 → **코드 3곳 선행 필요**(set:html + 인라인 포매터/HTML 저장 + `.curr-intro` mark·b 스타일).

## 수정 상세 (실행 지침)

### Phase 0 — 표기 인프라 (색·렌더 준비, 선행 필수)

색과 렌더를 먼저 갖춰야 이후 콘텐츠에 넣는 `<mark>`가 하늘색으로 보인다.

1. **하늘색 하이라이트 토큰 신설(권장):** `tokens.css`에 `--prose-hl`(라이트/다크) 추가 — 라이트 `#E8F3FF`(=blue-50) 계열, 다크는 저휘도 반투명 파랑. **스코어용 `--highlight`(초록)는 건드리지 않는다**(color_legend SSOT 유지). LessonView `.prose mark` 배경을 `--highlight`→`--prose-hl`로 교체.
   - (대안) `--highlight` 자체를 하늘색으로 바꾸는 안은 color_legend와 의미 충돌 → 미해결 질문 참조.
2. **커리큘럼 intro 렌더 활성화(3곳):**
   - `CurriculumView.astro:52` `<p>{p}</p>` → `<p set:html={…}>`로 전환.
   - intro 문자열을 인라인 포매터로 통과(예: `build-content.mjs`의 `inline()` 재사용해 `**…**`·`<mark>` 처리) **또는** intro에 HTML을 직접 저장(신뢰 소스).
   - `app.css`의 `.curr-intro`에 `mark`(=`--prose-hl`)·`b/strong` 스타일 추가.
3. **검증:** 파일럿 한 구절에 `<mark>`·`**볼드**`를 넣어 day·커리큘럼 양쪽에서 하늘색·굵게가 실제로 렌더되는지 확인(build + 육안).

### Phase 1 — 레시피 수립 (판단, 1회·파일럿에서만)

**파일럿 선정:** 커리큘럼 1개의 intro + 그 커리큘럼의 대표 1주(4 day × 3언어). 여기서만 "무엇을 강조할지"를 사람이 판단하고, 결과를 **아래 레시피 규칙으로 동결**한다.

### ★ 동결 레시피 (2026-07-09 파일럿에서 확정 — 집행자는 이 규칙만 따른다)

**★ A 방식(2026-07-09 재확정): 볼드+하이라이트 겹침 허용 + 문법 경계.** 이전 "볼드 회피" 규칙은 하이라이트를 핵심어 옆 어정쩡한 술어로 밀어내 어색했다 → 폐기.

**day 페이지 (하이라이트만, `<mark>…</mark>`):**
- **대상:** 각 산문 문단의 **핵심 키워드/구절** = 그 문단이 말하는 개념·행동지시의 알맹이. **볼드(`**…**`)인 어구여도 그게 핵심이면 그 위에 겹쳐 하이라이트**한다(`**<mark>3도</mark>**` 형태). 통상적 "굵게+형광펜"처럼 강조를 한 곳에 모은다.
- **밀도:** 문단당 **1개**(핵심이 뚜렷이 둘인 긴 문단만 최대 2). 짧은 문단·필러 문단은 0개 허용.
- **★경계(문법 단위 — 어색함 방지):** 하이라이트는 **문법적으로 자족적인 단위**여야 한다. 둘 중 하나:
  - **(A) 의미가 자족하는 명사구(2어절 이상 권장):** `코드의 표정`, `세 종류의 음`, `도미넌트의 긴장`, `표정을 쥔 음` — 짧고 단어 안 잘림. **1순위.**
  - **(B) 결론 동사까지 포함한 짧은 절:** `3도가 정해요`, `세 음밖에 없어요` — 서술어까지 완결.
  - **★바 단일 상용어 지양:** `표정`·`긴장`·`색` 같은 **맥락 없이 한 단어**만 칠하면 아쉽다 → 그 단어를 감싼 명사구로(`코드의 표정`, `도미넌트의 긴장`). 단 `3도`·`빈 피킹`·`ii-V-I`처럼 **그 자체로 특정적인 전문 용어**는 단일어 허용.
  - **금지:** 단어 중간 절단(`없|거든요`, `시작|돼요`), 조사 절단·동사 버리기(`…3도가|정해요`), 문장 통째(15자↑), **맥락 없는 단일 상용어**.
- **제외:** 헤딩, ②악보 캡션("**예제 N —**" 설명), 링크, 필러("당연해요/괜찮아요" 격려), **③'오늘의 완료 기준' 줄(요약·판정 문장이라 하이라이트 안 함)**.
- **섹션별:** ①이론=핵심 개념/원리어, ③연습=각 시간블록의 **목표어**(예: "빈 피킹", "3도 착지") — **단 '오늘의 완료 기준' 줄 제외**, ④팁=흔한 실수의 **핵심어**. ②시각=산문 문단 있으면 동일, 캡션 제외.

**커리큘럼 intro (하이라이트 + 볼드):**
- **볼드(`**…**`):** 문단당 **1개** — 코스를 정의하는 용어/시스템명/기간(명사구). 예: "8주 코드 조립 시스템", "9도 텐션".
- **하이라이트(`<mark>`):** 문단당 **1개** — 그 문단의 **성과/약속 핵심어**. 볼드와 **같은 어구여도, 다른 어구여도 무방**(핵심이면 겹쳐도 OK). 문법 경계 규칙 동일 적용.

**3언어 일관성:** ko에서 잡은 구절의 **의미 등가 구절**을 en/ja에도 동일 위치·개수로. 개수는 3언어 일치.

### Before/After (A 방식 표준 예시 — 흉내낼 것)

**커리큘럼 intro 문단1:**
- After: `…분을 위한 **<mark>8주 코드 조립 시스템</mark>**이에요.` (핵심 시스템명에 볼드+하이라이트 겹침) 또는 볼드=시스템명·하이라이트=`코드의 원리`(성과어) 분리도 가능.

**day ①이론 (볼드 위에 겹침):**
- Before: `…코드의 **표정**을 결정하는 음. 밝은가 어두운가는 전부 이 3도가 정해요.`
- After: `…코드의 **<mark>표정</mark>**을 결정하는 음. 밝은가 어두운가는 전부 이 **<mark>3도</mark>**가 정해요.` (핵심 명사 `표정`·`3도`에 볼드+하이라이트 — 술어로 밀어내지 않음)

**day ①이론 (볼드 없는 문단):**
- Before: `…이 커다란 코드 안에도 사실 세 종류의 음밖에 없거든요.`
- After: `…이 커다란 코드 안에도 사실 <mark>세 종류의 음</mark>밖에 없거든요.` (깔끔한 명사구, 단어 안 잘림)

### Phase 2 — 실행 (기계적 배치, 판단 재개 금지)

동결된 레시피만 보고 배치 단위로 적용한다. **강조 기준을 다시 판단하지 않는다.**

- **배치 단위:** 커리큘럼별 **주(week) 단위**(4 day × 3언어 = 12파일) 권장 — 프로젝트의 주/월 배치 관례와 정합. 더 촘촘히 원하면 day(3파일) 단위.
- **각 배치 절차:** 레시피대로 스캔 → `<mark>`(day) / `<mark>`+`**볼드**`(커리큘럼) 적용 → V1 build → 다음 배치. 3언어 등가 배치 확인.
- **진행 추적:** 아래 체크리스트를 커리큘럼·주 단위로 채운다.

## 기존 사이트/배포 불변 보장

- **콘텐츠 가법:** `<mark>` 삽입은 산문 HTML만 바꾼다. **악보 JSON 블록 불변** → `check-invariants.mjs`(스코어 블록 지문) 회귀 0.
- **day 렌더 무변경:** mark 스타일이 이미 존재 → Phase 0의 색 토큰 교체 외 렌더 코드 불변. mark 없는 기존 문단은 그대로.
- **커리큘럼 렌더:** intro가 없는 커리큘럼(향후)·intro에 강조 없는 경우 모두 기존과 동일하게 렌더(가법 분기).
- **Zero-Cost·i18n·토스톤·계층 구조** 불변([D]군).

## 검증 방법

- **V1** `cd web_app && npm run build` exit 0.
- **V2** 배치 편집 시 ko/en/ja 산문 강조가 등가 구절에 정합(개수·위치 대응). 악보 JSON 블록 3언어 바이트 동일 유지.
- **V3** `node web_app/scripts/check-invariants.mjs` 회귀 0(산문 강조는 스코어 지문에 무관).
- **육안:** day·커리큘럼 페이지에서 하이라이트=하늘색, 커리큘럼 볼드=굵게, 라이트/다크 모두 가독. 밀도 상한 준수(문단당 1~2).

## 주의 / 정지조건

- **색 충돌:** 스코어 `--highlight`(초록, color_legend SSOT)와 산문 하이라이트를 **분리**해야 한다. `--highlight`를 통째로 하늘색으로 바꾸면 스코어 role "highlight"까지 하늘색이 되어 SSOT가 깨진다 → 별도 토큰 필수.
- **커리큘럼 intro는 코드 선행:** set:html 전환 없이 콘텐츠에 `<mark>`/`**`만 넣으면 **글자로 노출**된다. Phase 0을 반드시 먼저.
- **set:html 안전성:** intro는 신뢰 소스(우리 저작)라 XSS 우려는 낮으나, 외부 입력을 intro에 넣지 않는다는 전제 유지.
- **밀도 폭주 금지:** 강조가 많아지면 강조가 아니다. 문단당 상한을 레시피에서 못 박고 집행자가 넘기지 않게.
- **레시피 미동결 상태로 Phase 2 착수 금지:** 판단을 분리한 이유가 무의미해진다. 파일럿·규칙 확정 전엔 실행 배치를 시작하지 않는다.
- **3언어 드리프트:** 한 언어만 강조하고 나머지를 빠뜨리면 정합 깨짐 — 배치는 항상 3언어 묶음으로.

## 체크리스트

**Phase 0 — 인프라(선행)**
- [x] `--prose-hl` 하늘색 토큰(라이트/다크) 신설, `.prose mark` 배경 교체 — 착수 전 기 완료 상태 확인(2026-07-09 파일럿 시점 렌더 정상 확인)
- [x] `CurriculumView` intro `set:html` 전환 + 인라인 포매터/HTML 경로 — 상동
- [x] `.curr-intro` mark·b/strong 스타일 추가 — 상동
- [x] 파일럿 구절로 day·커리큘럼 양쪽 렌더 확인(build+육안) — 2026-07-09, `npm run build` 산출물에서 day/intro 3언어 `<mark>` 개수 육안+grep 확인

**Phase 1 — 레시피 동결**
- [x] 파일럿(커리큘럼 1 intro + 1주 4day×3언어) 선정·강조 판단 — chord_building_2months intro + month_1/week_1 day1-4, 2026-07-09
- [x] 동결 레시피(대상·밀도상한·제외·볼드규칙·언어일관성) 이 문서에 확정 기재 — 위 "★ 동결 레시피" 섹션
- [x] Before/After 표준 예시 남김 — 위 섹션

**Phase 2 — 실행(배치)**
- [x] chord_building intro 1종 × 3언어 (하이라이트+볼드, A방식 재적용) — 2026-07-09, ko는 파일럿에서 A방식 적용됨; en/ja를 A방식으로 신규 반영(문단당 볼드+하이라이트 겹침 1개 — 8-week chord-building system / voice-leading / record your own comping 등가 3언어)
- [x] chord_building month_1/week_1 (day 1~4) × 3언어 (하이라이트, A방식 재적용) — 2026-07-09, 구 B방식(볼드 회피·술어 절단) 파일럿을 폐기하고 A방식(볼드 겹침 허용 + 명사구 우선 문법 경계)으로 12파일 전체 재작성. day별 `<mark>` 개수: day1=16/16/16, day2=15/15/15, day3=15/15/15, day4=15/15/15(ko/en/ja). 악보 JSON 블록 3언어 바이트 HEAD 대비 불변 확인, `<mark></mark>` 태그 balance 확인, check-invariants 315블록 회귀 0.
- [ ] chord_building 나머지 28 day × 3언어 (하이라이트, A방식) — 남은 7주
- [ ] solo_scale intro 1종 × 3언어 + 52 day × 3언어 (하이라이트) — 주 단위 배치
- [ ] funk_rhythm intro 1종 × 3언어 + 32 day × 3언어 (하이라이트) — 주 단위 배치
- [x] V1~V3 통과(파일럿 범위) + Roadmap·이 문서 갱신 — 2026-07-09

## 미해결 질문 (사람 결정)

- **하늘색 정확한 값** — `#E8F3FF`(blue-50) 그대로 vs 더 연하게/진하게. 다크모드 대응 값.
- **토큰 전략** — 신규 `--prose-hl`(권장, 스코어 초록과 분리) vs `--highlight` 재정의(color_legend까지 갱신해야 함).
- **커리큘럼 intro 저장 형태** — intro에 HTML 직접 저장 vs `inline()` 포매터로 `**…**`·`<mark>` 변환(저작 편의 ↑).
- **하이라이트 밀도 상한** — 문단당 1개 고정 vs 최대 2개 허용. 섹션당 상한.
- **볼드/하이라이트 관계** — 겹침 금지(택1) vs 허용. 커리큘럼에서 볼드=용어/하이라이트=약속 문장 구분 확정.
- **파일럿 범위** — 어느 커리큘럼·어느 주를 대표로 할지.
- **배치 단위** — 주(4day×3언어) vs day(3언어).
