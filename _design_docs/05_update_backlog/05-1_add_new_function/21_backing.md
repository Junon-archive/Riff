---
id: 21-backing
status: TODO
priority: high
risk: medium
depends_on: [20-metronome]
owner: null
---

> 🔴 TODO (2026-07-24). **설계 확정 + 프롬프트 뱅크 3세트(드럼비트·기타·베이스) 83트랙 저작 완료 · 음원 생성/구현 대기.** 매트릭스·용량·조 확정(커리큘럼 조 검증 반영). 다음 실물 준비물: **[`backing_prompts/`](backing_prompts/README.md) 로 Suno Pro 음원 생성 → 루프 트림 → 매니페스트.**
> **공통 셸(FAB·플로팅 오버레이·Segmented Control·전용 페이지·랜딩 진입·부드러운 전환·지연 로드·레슨 이동 시 정지)은 `20_metronome.md` PART A 가 SSOT.** 이 문서는 배킹 고유(음원 매트릭스·플레이어·매니페스트)만 정의. 릴리스는 메트로놈 다음(후속).

# 21 · 배킹 트랙 (Backing Track)

## 목적 (왜)

장르·조·템포별 **반복 반주**를 즉시 재생해 그 위에 연습. **엄청 단순·직관적** — 선택창 없이 버튼으로 바로 고르고 재생. Toss 톤·Zero-Cost·i18n·기타(파랑)/베이스(초록) 유지. 기타용/베이스용 분리.

## 확정 결정 (사람 확정 2026-07-24)

| 항목 | 결정 |
|---|---|
| 기타/베이스 | **분리**(각각 별도 세트). **기타 먼저 → 베이스 후속**(단계 배포). |
| 음원 출처 | **Suno Pro**(상업/재배포 라이선스 有 — 하단 T5). |
| 세트 구성 | **3세트**(R3): ① **드럼만(드럼비트) 11트랙**(3필·공유) ② **기타 36** ③ **베이스 36**. 총 **83트랙 / ~42MB**. |
| 장르 | 드럼비트(3필: 스트레이트·펑크16·셔플) · 블루스 · 락/팝 · 펑크 · R&B/네오소울 · 재즈 ii-V-I. |
| **조(key) 원칙** | **모든 키드 장르에 Am(솔로/스케일 커리큘럼 조) 포함** + **각 장르는 연계 커리큘럼이 다루는 조를 반드시 포함**. (커리큘럼별 조는 「매트릭스」 표에서 검증·확정) |
| 스케일 | **조(key)에 라벨로 병기**(별도 선택축 아님). 예: A → "A minor pentatonic". |
| 템포 | 키드 트랙은 **이산 3개**(느림/보통/빠름), **별도 파일**(피치 보존). **드럼비트는 3필 × 필별 템포셋**(R3, 스트레이트는 200 BPM 포함). |
| 루프 길이 | **1 전체 폼**: **블루스=12마디 필수**, 그 외 8~16마디. (초는 BPM 종속 — R1) |
| 포맷 | **AAC-LC .m4a 128k 스테레오** 확정. |
| 총 예산 | **≤ ~80 MB**로 시작, 부족하면 향후 상향. |
| 메트로놈↔배킹 | **상호 배타**(하나 켜지면 다른 것 정지). |
| 진입/셸 | 20_metronome PART A 공통(FAB 오버레이의 Segmented "배킹" 탭 + 전용 페이지 + 랜딩 "반주" 카드). |

## 플랫폼 사실(확인됨) — 용량 판단 근거

- **Cloudflare Pages(Free):** 사이트당 **파일 20,000개**, **파일당 최대 25 MiB**. 총량 하드캡은 이 둘뿐(개수는 우리에게 전혀 문제 없음 — Functions 음원 다 합쳐 수십~수백 개).
- **진짜 제약 = GitHub 저장소 비대화:** 배포가 GitHub→CF Pages라, 오디오(바이너리)가 repo를 무겁게 함(클론 느려짐). GitHub는 파일 >100MB 차단·>50MB 경고·repo <1GB 권장. → **총 오디오 예산을 스스로 절제**하는 게 관건.
- (미래 탈출구) 커지면 **Cloudflare R2**(무료 10GB)로 음원만 분리 가능. 단 새 서비스·복잡도 증가 → v1은 **repo 내 유지**(Zero-Cost·단순).

## ★ 매트릭스·용량 (확정안 — 커리큘럼 조 검증 반영)

> 목표: **최대한 다양 × 최대한 가볍게 × 밤티 안 나게 × 커리큘럼과 조 일치**.

### R1. 포맷·루프 길이
- **AAC-LC(.m4a), 스테레오, ~128 kbps** 확정. Safari/iOS 포함 전 브라우저 `decodeAudioData` 호환. ≈ **~1 MB/분**.
- **루프 = 1 전체 폼**(파일 안에 1회, Web Audio가 심리스 반복). **블루스=12마디 필수**, 그 외 8~16마디, **드럼만=16마디(8·16마디째 변주 — 필/변형으로 밤티 방지)**.
- **마디→초(4/4, `bars×4×60÷BPM`):** 12마디 = 70bpm **41s** · 90 **32s** · 120 **24s**. 16마디 = 70 **55s** · 110 **35s** · 200 **19s**. 8마디 = 70 **27s** · 100 **19s**. → 한 폼이 **~19~55초**, 트랙당 **~0.3~0.9 MB**(최장 드럼 16마디 70bpm ≈ 55s×128k ≈ 0.9MB).

### R2. 템포는 **별도 파일** (피치 보존)
- `playbackRate` 템포 변경은 **피치(조) 변함** → 금지. 실시간 타임스트레치(WASM)는 품질/무게 손해 → v1 미채택.
- **키드 트랙은 이산 3템포**(느림/보통/빠름) = 별도 렌더 파일. **드럼비트는 3필**(스트레이트/락·펑크16·셔플)로, 필마다 템포셋(R3 — 스트레이트 5개·나머지 3개).

### R3. 최종 매트릭스 (드럼만 · 기타 · 베이스 = 3세트)
- **축:** `장르 × 조(key) × 템포`. **스케일은 조에 라벨 병기**(선택축 아님).
- **조 원칙(확정):** ① **모든 키드 장르에 Am**(solo_scale가 Am 기반임을 검증) ② **각 장르는 연계 커리큘럼 조를 포함**.
- 커리큘럼 조 검증 결과: solo=**Am**(+C) · blues=**A** · funk=**E** · chord=**C**(+G) · neosoul=**Dm** · 입문일렉=**E/Em** · walking=**F** · slap=**Em** · shuffle=**E** · 입문베이스=**E**.
- **배포 순서:** 기타 세트 먼저 → 베이스 후속. **드럼만은 양쪽 공유**라 먼저 확보.

**① 드럼만(드럼비트) 세트 — 조 없음 · 기타/베이스 공유 · 순수 드럼 키트 · 16마디(8·16마디 변주).**

| 필(feel) | 템포(BPM) | 트랙 | 성격 · 연계 |
|---|---|---|---|
| 스트레이트/락 백비트 | 70·95·120·150·200 | 5 | 범용 백비트(락/팝, 전 장르 기본 타이밍) |
| 펑크 16비트 | 90·100·110 | 3 | 16분 고스트 그루브(funk·slap) |
| 셔플/블루스 스윙 | 70·90·120 | 3 | 트리플렛 셔플(blues·shuffle) |

- **드럼만 합계 = 11트랙.** 파일명 `drums/{feel}_{bpm}.m4a`(예: `straight_120`·`funk_100`·`shuffle_90`).

**② 기타 세트 — 리드 기타 제외(드럼+베이스+코드 컴핑 남음).**

| 장르 | 조(key) | 템포(BPM) | 트랙 | 커리큘럼 연계 · 조 근거 |
|---|---|---|---|---|
| 블루스(셔플) | A, E | 70·90·120 | 6 | blues(A)·shuffle(E). **A블루스=A마이너펜타 → solo/scale Am 겸용** |
| 락/팝 | Am, E, G | 80·110·140 | 9 | 입문일렉(E)·solo(Am)·G(팝 스테이플) |
| 펑크 | Am, Em | 90·100·110 | 6 | funk(E)·slap(Em)·solo(Am) |
| R&B/네오소울 | Am, Dm | 70·85·100 | 6 | neosoul(Dm)·solo(Am) |
| 재즈 ii-V-I | Am, C, F | 100·130·160 | 9 | chord(C)·walking(F)·solo(Am ii-V-i) |

- **기타 합계 = 36트랙.** 파일명 `guitar/{장르}_{조}_{bpm}.m4a`.

**③ 베이스 세트 — 베이스 제외(드럼+코드/기타 컴핑 남음) · 기타와 장르·조·템포 동일(미러).**

| 장르 | 조(key) | 템포(BPM) | 트랙 | 조 근거(베이스 커리큘럼) |
|---|---|---|---|---|
| 블루스(셔플) | A, E | 70·90·120 | 6 | shuffle베이스(E)·blues(A)·Am 겸용 |
| 락/팝 | Am, E, G | 80·110·140 | 9 | 입문베이스(E)·solo(Am) |
| 펑크 | Am, Em | 90·100·110 | 6 | slap베이스(Em)·solo(Am) |
| R&B/네오소울 | Am, Dm | 70·85·100 | 6 | neosoul 그루브(Dm)·solo(Am) |
| 재즈 ii-V-I | Am, C, F | 100·130·160 | 9 | walking베이스(F)·solo(Am ii-V-i) |

- **베이스 합계 = 36트랙.** 파일명 `bass/{장르}_{조}_{bpm}.m4a`. 베이스 핵심조(**E·Em·F**) 전부 포함.

- **총계 = 11 + 36 + 36 = 83트랙 / ~42 MB** — **예산 80MB 내 여유**(절반 근처). 향후 조/필/장르 확장 가능(메트로놈 샘플 포함해도 안전).

### R4. 파트 제외(분리의 실제 의미) — Suno 프롬프트에 반영
- **기타 배킹:** 드럼+베이스+코드, **리드 기타 제외**(사용자가 솔로).
- **베이스 배킹:** 드럼+코드/기타, **베이스 제외**(사용자가 베이스).
- **드럼만:** 드럼 단독(양쪽 공유 가능).

### R5. UX — 캐스케이딩(없는 조합이 안 생김)
- 버튼 UI는 **매니페스트 주도**. **장르 선택 → 존재하는 조만 활성 → 선택 조에 존재하는 템포만 활성**(계단식). → "없는 조합" 상태 자체가 발생하지 않음(C7 자연 해소). 회색 비활성 최소화.
- 로딩: 선택+재생 시 on-demand fetch→decode, **스피너/은은한 진행** 표시. 첫 재생 후 캐시.

## 기술 검토

### T1. 심리스 루프 — 갭 없이
- 단일 `<audio loop>`·mp3는 인코더 패딩 갭으로 "툭툭" 끊김. → **Web Audio 디코드 + `AudioBufferSourceNode.loop=true` + 정확한 `loopStart/loopEnd`**(샘플 정확 심리스). 코어 오디오 엔진은 메트로놈과 공유 가능(같은 `AudioContext`·마스터 게인).

### T2. Suno에서 "루프"를 뽑는 실제 비용 (프로덕션 주의)
- Suno는 **곡(인트로/아웃트로 有)**을 생성하지 곧바로 루프를 주지 않음. → 각 트랙마다 **안정적 반복 구간 선별 → 마디 경계/제로크로싱 트림 → 시작·끝 위상 정합(크로스페이드 또는 loopStart/End)** 편집 필요. 트랙당 수작업 발생.
- 완화: 프롬프트에서 **steady vamp·최소 인트로/아웃트로·일정 BPM·루프 지향** 지시. 그래도 검수/트림은 필요 → 작업량 산정에 포함.

### T3. 데이터 주도 매니페스트 (모듈형 원칙)
- `src/content/backing.json`(가칭): 트랙 배열 `{ id, instrument:"drums"|"guitar"|"bass", genre(또는 drums의 feel), key|null(드럼비트는 null), scale(label), bpm, file, loopStart, loopEnd, credits, license:"Suno-Pro" }`. → **트랙 추가 = 데이터+파일만**(코드 diff 0). 빌드 파이프라인(`scripts/build-content.mjs` 계열)에서 검증·복사.
- 이 매니페스트가 곧 캐스케이딩 버튼 UI의 소스.

### T4. 캐싱 / PWA
- 배킹 음원은 **SW precache 제외**(오프라인 설치 비대화 방지) → **런타임 캐시만**(백로그 07 App Shell 패턴 정합). `public/_headers`에 오디오 immutable 캐시 규칙 추가 고려.

### T5. Suno Pro 라이선스 (확인됨 2026-07)
- **Pro/Premier 구독 중 생성물 = 상업 사용 라이선스 부여, 구독 종료 후에도 유지** → 무료 공개 사이트 호스팅 가능.
- 주의: 2026 약관에서 "소유권(Ownership)" 문구 축소 — Suno가 형식상 "author", 사용자에겐 영구 상업 이용 라이선스. **100% AI 산출물은 저작권 등록 대상 아님**(제3자 재사용 막을 근거 약함) — 우리의 무료 배포·연습용 사용엔 무관.
- **기록 의무:** 각 트랙 `license:"Suno-Pro"` + 생성일/프롬프트를 매니페스트/프롬프트 뱅크에 남김.

## Suno 프롬프트 뱅크 → **`backing_prompts/` 로 분리** (2026-07-24 · 3세트 83트랙 저작 완료)

> 📁 **SSOT: [`backing_prompts/README.md`](backing_prompts/README.md)** — 인덱스 · 공통 작성 규칙 · 공통 Suno 설정 · **확장자/저장 경로** · **「사람이 순서대로 따라 하는 방법」(STEP 0~10 워크스루)** · 진행 체크리스트.
> 프롬프트 본문은 이 문서에 인라인하지 않는다(길고 복사·붙여넣기 대상이라 파일 단위가 맞다).

- **파일 규약:** md 1개 = (세트 × 장르 × 조). 드럼비트는 조가 없어 **md 1개 = 필(feel) 1개**. 같은 조합의 템포들은 프롬프트가 동일하고 BPM만 다르므로 한 파일에 **「렌더 타깃」** 표로 묶는다. md 파일명 = 오디오 파일명 접두사.
- **오디오 파일명:** 키드 트랙 `{장르슬러그}_{조}_{bpm}.m4a`(슬러그 `blues` `rock` `funk` `neosoul` `jazz`) · 드럼비트 `{feel}_{bpm}.m4a`(feel `straight` `funk` `shuffle`). 예: `blues_A_90.m4a` · `jazz_F_130.m4a` · `straight_120.m4a`. **기타/베이스는 파일명이 같고 디렉터리로 구분**(매니페스트는 `instrument` 필드).
- **배치 경로(권장안, 구현 세션 확정 대기):** `web_app/public/audio/backing/{drums|guitar|bass}/{파일}.m4a` → URL `/audio/backing/guitar/blues_A_90.m4a`. 기존 `public/curriculum/*.webp`·`public/icons/` 관례를 따랐고, `audio/` 로 한 겹 묶어 메트로놈 CC0 샘플(`audio/metronome/`) 자리를 미리 확보. **최종 정본은 위 T3 매니페스트(`backing.json`)의 `file` 필드** — 바뀌면 `backing_prompts/README.md` 「저장 경로」도 같이 고친다.
- **작업 원본 WAV는 repo 밖**에 둔다(83트랙 WAV는 수 GB → 저장소 비대화). 배포용 `.m4a` 만 커밋.
- **3세트 27파일 / 83트랙 전부 저작 완료.** 각 md에 스케일 라벨·폼(마디)·제외 파트·코드 진행·영문 스타일 프롬프트·Exclude Styles·루프 트림 메모·**생성 로그(생성일/링크/라이선스 기록란)** 포함.
- **폼 길이 확정:** 블루스 12마디 · 그 외 16마디 · 드럼비트 16마디(8·16마디째 필).
- **용량 재산정 ≈ 49 MB** (드럼 6.7 + 기타 21.0 + 베이스 21.0, 총 ≈50.6분). R3 추정 ~42MB 대비 상향 — **비블루스 폼을 8마디가 아닌 16마디로 확정**했기 때문(R1 "8~16마디" 범위 내). 8마디면 재즈 160BPM에서 루프가 12초라 반복 티가 심하다. **예산 80MB의 61%** 로 여유 있고, 부족하면 비블루스를 8마디로 줄여 ~10MB 회수 가능.
- **베이스 세트는 [`backing_prompts/bass/README.md`](backing_prompts/bass/README.md) 가 서브 인덱스** — 우선순위 Tier 1~3(각 12트랙, Tier 1 = 커리큘럼 직결 E·Em·F) + **★ 베이스 세트 공통 함정**(Exclude `bass` 로 킥까지 얇아짐 · 피아노/로즈 왼손이 베이스 라인을 대신 걸음 · 기타 저음현 부기).

| 세트 | 파일 | 트랙 | 용량 |
|---|---|---|---|
| **드럼비트** `drums/` | `straight.md` · `funk.md` · `shuffle.md` | 11 | ≈ 6.7 MB |
| **기타** `guitar/` | `blues_A/E` · `rock_Am/E/G` · `funk_Am/Em` · `neosoul_Am/Dm` · `jazz_Am/C/F` (12) | 36 | ≈ 21.0 MB |
| **베이스** `bass/` | 동일 구성 미러 (12) | 36 | ≈ 21.0 MB |
| **합계** | **27 파일** | **83** | **≈ 49 MB** |

> 세트별 상세 매트릭스는 위 **★R3**, 파일 단위 인덱스는 `backing_prompts/README.md` 를 본다.

## 결정 완료 (2026-07-24) — 매트릭스 확정

- **3세트 = 드럼비트 11 + 기타 36 + 베이스 36 = 83트랙** · 조 = **Am 공통 + 커리큘럼 조**(위 ★R3) · **락/팝 G 유지** · **블루스 A = Am 소울 겸용**(별도 Am 트랙 없음) · 스케일 = 조 라벨 · 키드 템포 3개 · **드럼비트 3필(스트레이트 5템포·펑크 3·셔플 3), 16마디 8·16마디 변주** · 배포는 드럼비트/기타 먼저 → 베이스 후속(**프롬프트는 3세트 모두 저작 완료**) · 루프 1폼(블루스 12마디·그 외 16마디) · AAC 128k · 예산 ≤80MB(실측 목표 ≈49MB) · 메트로놈과 상호 배타.

### 장르 확장 후보 (v1 미포함 — 나중 검토용)

- **메탈**(드롭튜닝 리프·Em/Dm), **보사노바**(Am/Dm 라틴), **어쿠스틱 발라드**(G/C 스트럼), **레게**(오프비트), **시티팝/재즈펑크** 등. 예산(80MB)에 여유가 크므로 수요 보이면 매니페스트에 데이터+파일만 추가하면 됨(R6 모듈형).

## 남은 확인 사항 (배킹) — 지금 결정 불필요

- (베이스 단계에서 재확인) 베이스 세트 조 구성: E/Em·F·Am 중심으로 좁힐지 — 기타 배포 후 논의.

## 검증 게이트 (착수 후)

- V1 `npm run build` exit 0 · `astro check` 0.
- V2 지연 로드 — 미사용 페이지 번들에 플레이어/음원 미포함.
- V3 심리스 루프 — 이음새 갭 청감 0.
- V4 매니페스트 주도 — 신규 트랙 추가가 데이터+파일만(코드 diff 0).
- V5 용량 예산 준수(총 오디오 ≤ 합의값) + 각 트랙 credits/license 기록.
- V6 캐싱 — 배킹 음원 SW precache 제외 확인.
- V7 i18n 3언어 라벨 누락 0 · 기타/베이스 테마.

## 체크리스트

- [x] 「결정 대기 질문」 답변 → 매트릭스·예산 확정 (2026-07-24)
- [ ] 트랙 매니페스트 스키마 확정(`backing.json`) + 빌드 검증
- [x] **Suno 프롬프트 뱅크 작성 — 3세트 83트랙(27파일) 전부 완료** (2026-07-24, `backing_prompts/`)
- [ ] 음원 생성(사람 · Suno Pro) — ① 드럼비트 11 → ② 기타 36 → ③ 베이스 36
- [ ] 루프 트림/심리스 검수 파이프라인(수작업+헬퍼)
- [ ] 플레이어(공유 AudioContext·심리스 루프) + 캐스케이딩 버튼 UI
- [ ] 오버레이 배킹 탭 + 전용 페이지(3언어) + 랜딩 "반주" 카드
- [ ] 캐싱(런타임만)·용량 검증
- [ ] Roadmap.md 갱신 + 본 문서 status→DONE
