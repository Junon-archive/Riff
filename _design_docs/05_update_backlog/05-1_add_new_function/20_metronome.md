---
id: 20-metronome
status: TODO
priority: high
risk: medium
depends_on: []
owner: null
---

> 🔴 TODO (2026-07-24). **설계 확정 완료 · 구현 대기.** 모든 결정 마감(아래 확정 표들). 이 문서 하나로 다른 CC 세션이 바로 착수 가능. 다음 실물 준비물: **CC0 드럼(킥·스네어·하이햇)·목소리(One~) 샘플 확보**.
> **릴리스 순서:** 메트로놈 = **Functions 1호**(자산 0·리스크 낮음) → 먼저 배포. 배킹(21)은 후속. 이 문서가 **Functions 공통 셸(FAB·오버레이·전용 페이지)의 SSOT** — 21_backing 은 이 셸을 참조만 한다.

# 20 · 메트로놈 (Metronome) + Functions 공통 셸

## 용어

- **Functions:** 커리큘럼 외 추가되는 연습 도구 묶음(메트로놈·배킹·튜너·루프스테이션…)의 총칭.
- 이 문서는 (A) **Functions 공통 셸**과 (B) **메트로놈 기능**을 함께 정의한다.

## 목적 (왜)

연습 중 **커리큘럼 페이지를 떠난 느낌 없이** 바로 켜고 끄는, 소리가 불쾌하지 않은 메트로놈. Toss 톤(미니멀·깔끔)·Zero-Cost(정적, 서버·DB 없음)·i18n 3언어·기타(파랑)/베이스(초록) 테마를 그대로 지킨다.

---

## PART A · Functions 공통 셸 (SSOT — 21_backing 도 이걸 따름)

### A0. 확정 결정 (사람 확정 2026-07-24)

| 항목 | 결정 |
|---|---|
| 랜딩 진입 | 커리큘럼 카드 **위에 작은 카드 4개 한 행**: 튜너 · 메트로놈 · 반주 · 루프. 각 카드 클릭 = **각 도구 전용 페이지로 새로 이동**(플로팅 아님). |
| 전용 페이지 구조 | **도구마다 독립 페이지**(허브 1개 아님). 미래 튜너·루프 확장 고려. |
| 레슨 세부 내 진입 | **우하단 FAB**. 누르면 **페이지 전체 전환이 아니라 플로팅 오버레이**로 Functions가 뜸(커리큘럼에서 나간 느낌 X). |
| 오버레이 구성 | 오버레이 **맨 위 Segmented Control**(메트로놈 / 배킹) → 선택 시 아래 패널 교체. 교체도 **부드러운 전환 이펙트**. |
| 재생 지속 범위 | 오버레이에서 재생 후 닫아도 **같은 페이지 안에서는 계속 재생**. 재생 중엔 **FAB 아이콘이 달라짐**(재생 상태 표시). |
| 레슨 이동 시 | **정지(새로 켜기)**. 다음/이전 레슨으로 넘어가면 소리 끊고 초기화 → `transition:persist` 불필요(구현 단순화). |
| 전환 이펙트 | Functions 관련 **모든 페이지·패널 이동은 딱딱하지 않게** 기존 View Transitions 톤으로 부드럽게. |
| 테마 | `riff_instrument`(기타 파랑/베이스 초록) 액센트 재사용. |
| i18n | 모든 라벨 KR/EN/JP. (메트로놈 목소리 카운트만 영어 고정) |

### A1. 아키텍처 — "레슨 이동 시 정지"가 설계를 단순화한다

- 사이트는 `<ClientRouter/>`(Astro View Transitions) 소프트 내비. **레슨 이동 시 정지가 요구**이므로, 오디오 엔진을 전환 간 살려둘 필요가 없다 → **`transition:persist`·전역 싱글턴 지속 로직 불필요**.
- **정지 배선:** 오디오 엔진 모듈 싱글턴을 두되, `astro:before-swap`(또는 `astro:before-preparation`)에서 `engine.stop()` + FAB 상태 리셋. "같은 페이지 안 지속"은 DOM/JS 자연 수명으로 자동 충족(오버레이만 닫고 엔진은 계속 발음).
- **지연 로드(정적 구성 유지 핵심):** FAB·오버레이 골격은 가볍게 상주, **오디오 엔진 코드(스케줄러·합성·샘플 디코드)는 FAB 최초 클릭 시 `import()` 지연 로드**. → 메트로놈/배킹 안 쓰는 페이지뷰는 JS·오디오 비용 0. 지금의 가벼운 정적 체감 유지.
- **재사용 컴포넌트:** 메트로놈/배킹 UI는 **(a) 레슨 내 플로팅 오버레이**와 **(b) 전용 페이지** 양쪽에서 같은 코어를 쓴다. 셸(오버레이 vs 풀페이지)만 다르고 내부 패널은 공용.

### A2. FAB 노출 범위 (확정)

- **커리큘럼 세부/레슨 페이지에서만** FAB 노출. **랜딩 제외**(랜딩은 카드 행으로 진입). 전용 도구 페이지에도 FAB 불필요(이미 그 도구 화면).
- **재생 중 FAB 아이콘 = 이퀄라이저 바 애니메이션**(막대 위아래 움직임)으로 "울리는 중" 표시. 정지 시 기본 아이콘.

### A2.1 라벨 정책 (확정 — C1)

- **그룹 라벨("연습 도구/Tools") 안 씀.** 랜딩은 **아이콘+이름 카드 4개**(튜너·메트로놈·반주·루프)로 자명. 레슨 내 오버레이는 **Segmented Control(메트로놈 / 배킹)** 로 바로 구분 → 별도 헤더 라벨 불필요. (개별 카드/세그먼트 명칭만 i18n)

### A3. 관련 코드 위치 (검증됨)

| 무엇 | 위치 | 쓰임 |
|---|---|---|
| 클라이언트 JS 진입 | `web_app/src/scripts/app.ts` (`<script src>` in `Base.astro:162`) | FAB 바인딩·오버레이 open/close·`astro:page-load` 재바인딩·`astro:before-swap` 정지 배선 추가 지점 |
| 바텀시트/스크림 패턴 | `app.ts` `openSheet`/`closeSheet`(73·76), `.scrim` DOM | 오버레이 open/close·백드롭 UX 참고(플로팅 오버레이는 이 패턴 변형) |
| View Transitions | `Base.astro:156` `<ClientRouter/>` | 부드러운 전환 톤·`astro:before-swap` 훅 |
| 악기 테마 | `app.ts:~492` `riff_instrument`(기타 파랑/베이스 초록) | 액센트 색 |
| 상태 저장 | `web_app/src/lib/storage.ts`(`gh_state` 단일 키) | 마지막 BPM·볼륨·음색·박자 저장(래퍼 경유 필수) |
| i18n | `web_app/src/lib/i18n.ts` + ko/en/ja 사전 | 라벨 3언어 |
| 전용 페이지 라우팅 | `web_app/src/pages/`(`[lang]`·`c/…`) | 도구별 페이지 신설(ko 무접두, `/en/`·`/ja/` 접두, hreflang) |
| SW precache | `web_app/public/sw.js` | 메트로놈 CC0 샘플은 소형이라 앱셸 캐싱 가능(배킹 음원은 21에서 제외) |

---

## PART B · 메트로놈 기능 명세

### B0. 확정 결정 (사람 확정 2026-07-24)

| 항목 | 결정 |
|---|---|
| v1 음색 | **일반(합성 클릭)·드럼·목소리 3종 전제로 엔진 설계**. 합성 클릭은 v1 라이브. **드럼·목소리는 "파일만 넣으면 되는" 수준까지 구현**(샘플 슬롯·디코드·매핑 준비, 파일은 후속). |
| 드럼 음색 매핑 | **3종 키트**(퀄리티 우선): **킥**=강박(1박) · **스네어**=백비트(4/4의 2·4박) · **하이햇**=나머지 박·세분(여리게). 비4/4(3/4·6/8)는 스네어 위치를 미터별로 조정(구현 시 튜닝). 샘플 슬롯 3개: `drum/{kick,snare,hihat}.m4a`. |
| 목소리 겹침(빠른 BPM) | **180 BPM 이상 목소리 음색 자동 비활성**(그 이상 선택 시 합성/드럼 폴백 또는 안내). **임계값은 상수로 노출 → 향후 변경 가능.** |
| 박자 | **2/4, 3/4, 4/4, 6/8** 만. 자유 입력 N/M **미지원**. |
| 세분(subdivision) | **소리만 지원**(8분 / 셋잇단 / 16분 "매 세분 클릭"). **세분음 시각 이펙트 없음.** |
| 복잡 리듬(쉼표·n연음 시퀀서) | **v1 제외**(동의). |
| 목소리 카운트 | **영어 통일**(One·Two·Three·Four…). |
| BPM 범위 | **30 ~ 300**. |
| 탭 템포 | 지원(두드려 BPM 설정). |
| 시각 표시 | **박 단위만.** 박 번호 동그라미(1·2·3·4…) + **각 동그라미 아래 점이 현재 박에 점등**(은은한 글로우). **강박**엔 원 주변으로 **파동(ripple)** 퍼짐. **6/8은 8분 6개**(점 6개)로 표시. 세분음은 시각 없음. |
| 샘플 라이선스 | **CC0**. |

### B1. 컨트롤 구성 (잠정 레이아웃)

- 큰 **BPM 숫자** + −/＋ 및 슬라이더, **Tap 버튼**.
- **박자 선택**(2/4·3/4·4/4·6/8) 세그먼트.
- **세분 선택**(♩ / ♪♪ / 셋잇단 / 16분) 세그먼트.
- **음색 선택**(일반·드럼·목소리).
- **볼륨 슬라이더**(마스터 게인).
- **시각 박 표시**(원 N개, 강박 파동).
- **재생/정지**.
- 마지막 설정(BPM·볼륨·음색·박자·세분)은 `storage.ts` 경유 저장 → 다음에 그대로.

### B2. 기술 검토 — 타이밍(핵심)

- `setInterval`로 직접 발음 = **박 드리프트/백그라운드 throttle**. **금지.**
- **Web Audio 룩어헤드 스케줄러**(Chris Wilson "A Tale of Two Clocks"): 느슨한 `setInterval(~25ms)`가 미래 ~100ms를 미리 보고 각 박/세분을 `AudioContext.currentTime`(샘플 정확)에 **예약**. 하드웨어 클록이 타이밍 보장 → 드리프트 0.
- **시각 동기:** 예약된 `{beat, subdiv, time, isAccent}` 큐를 `requestAnimationFrame`이 읽어 `currentTime`이 지날 때 글로우/파동 트리거. (오디오와 별도 타이머로 그리면 눈·귀 어긋남.)

### B3. 기술 검토 — 음색(하이브리드)

- **일반 클릭 = 합성(자산 0KB):** `OscillatorNode`+`GainNode` 엔벨로프(빠른 attack·지수 감쇠). 강박=높은 음/큰 게인, 약박=낮은 음. 세분음은 더 여리게. → 예쁜 클릭을 파일 없이.
- **드럼·목소리 = 샘플:** 짧은 mono 파일을 빌드 자산으로 로드→`decodeAudioData`→`AudioBuffer` 캐시. 매 발음마다 **새 `AudioBufferSourceNode`**.
  - **"빠른 템포에서 목소리 겹침/잘림" 해결됨:** 매 박 새 노드라 샘플이 박 간격보다 길어도 **겹쳐 재생(끊김 없음)**. (단일 `<audio>` 방식의 문제였음.) 매우 빠를 때 처리(겹침 허용/하드컷/특정 BPM 이상 비활성)는 아래 Q.
- **샘플 슬롯 계약(파일만 넣으면 되게):** `sounds/{voice}/{1..N}.m4a`, `sounds/drum/{accent,beat,sub}.m4a` 식 규약 + 매핑 테이블 미리 정의 → CC0 파일 확보 시 드롭인.

### B4. 결정 완료 (2026-07-24)

- 목소리 겹침 → **(c) 특정 BPM↑ 자동 비활성**(임계값 튜닝, 잠정 ~180).
- 6/8 → **8분 6개 점**.
- 세분음 → **소리만**(시각 없음). 시각은 박 동그라미+아래 점 점등, 강박 파동만.
- 드럼 → **킥(강박)/하이햇(약박) 키트**. 세분 하이햇은 아주 여리게(또는 무음) — 실제 톤은 구현 시 튜닝.
- FAB → **커리큘럼/레슨 페이지 한정**(랜딩 제외).

### B5. 남은 미세 결정 (전부 확정 — 구현 시 튜닝만)

- 목소리 자동 비활성 임계 = **180 BPM**(상수, 변경 가능하게).
- 드럼 = **킥/스네어/하이햇 3종 키트**(백비트 스네어 포함) — 확정.
- 강박 파동 세기: Toss 톤 상 **아주 은은하게**가 기본(구현 시 시각 튜닝).

## 검증 게이트 (착수 후)

- V1 `npm run build` exit 0 · `astro check` 0.
- V2 **지연 로드** — 메트로놈 미사용 페이지 번들에 오디오 엔진 코드 미포함(번들 분석).
- V3 **타이밍** — 장시간(수 분) 재생 드리프트 육안/계측 0.
- V4 **정지 배선** — 레슨 이동 시 소리 정지 + FAB 상태 리셋 확인.
- V5 **오버레이 지속** — 같은 페이지 내 닫아도 재생 유지.
- V6 i18n 3언어 라벨 누락 0.
- V7 Toss 톤·기타/베이스 테마 육안 확인.

## 체크리스트

- [ ] 「결정 대기 질문」 답변 수합 → 확정 표 반영
- [ ] Functions 공통 셸 PoC(FAB·플로팅 오버레이·Segmented·부드러운 전환·지연 로드·before-swap 정지)
- [ ] Web Audio 룩어헤드 스케줄러 + 세분 + 시각 동기(글로우/파동)
- [ ] 합성 클릭 음색(강/약/세분)
- [ ] 드럼·목소리 샘플 슬롯/디코드/매핑(파일 대기 상태까지)
- [ ] 박자(2/4·3/4·4/4·6/8)·탭 템포·볼륨·설정 저장
- [ ] 전용 페이지(3언어) + 랜딩 카드 행 진입
- [ ] 검증 게이트 통과
- [ ] Roadmap.md 갱신 + 본 문서 status→(부분)DONE
